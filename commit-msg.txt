feat(sniper): AEGIS-SNIPER-01 — baseline engine + sniper rules engine

src/sniper/baseline.ts: BaselineEngine — Welford online algorithm for running
mean + variance, SQLite baselines.db, per-process per-context fingerprints,
getDeviation() returning z-scores and ratios, MIN_SAMPLES=20 reliability gate,
DEVIATION_ZSCORE_THRESHOLD=2.0, start/stop/flush, singleton factory

src/sniper/engine.ts: SniperEngine — 3 built-in rules (node-runaway 120s,
searchindexer-hog 60s, generic-runaway 300s), blast radius duration multipliers
(none=1x, low=1.5x, medium=2x, high=3x, critical=never), graduated action path
(throttle→suspend→kill), catalog gate on every action, context exemptions,
cooldown tracking, EventEmitter events (flagged/action_taken/recovered/deferred),
getActiveWatches() + getRules() + ingest()

scripts/aegis-worker.ps1: throttle_process_pid (CPU=Idle + IO=VeryLow via NtApi),
suspend_process_pid (NtSuspendProcess), kill_process_pid (Stop-Process -Force)

src/config/types.ts: sniper field added to SystemSnapshot

src/status/collector.ts: setSniperEngine(), recentSniperEvents buffer (20),
ingest() called each poll, sniper stats merged into snapshot

src/tray/lifecycle.ts: baseline + sniper init, action callbacks to worker IPC,
context_changed → setContext(), engine stopped on shutdown

src/status/server.ts: renderSniper() (watch count + color-coded action log),
sniper HTML section, called from render(d)

TypeScript: 0 errors
