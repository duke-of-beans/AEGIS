feat(context): AEGIS-CONTEXT-01 — context detection engine + composable policies

src/context/engine.ts: ContextEngine — PowerShell WinEvent poller via Win32
GetForegroundWindow p/invoke, weighted focus time accumulation per process,
8 context types (deep_work/build/research/meeting/media/gaming/idle/unknown),
rule evaluator with min_focus_sec thresholds, 20% weight decay every 30s,
EventEmitter context_changed events, auto-respawn on subprocess exit

src/context/policies.ts: PolicyManager — composable policy stack (base + overlays),
BUILTIN_POLICIES (browser-default, searchindexer-throttle, diagtrack-throttle),
CONTEXT_OVERLAY_TEMPLATES per context (deep_work aggressive browser+services,
build CPU elevation, meeting tab suspension, gaming service stop),
pushOverlay/popOverlay/applyContextOverlays/pruneExpired methods

src/config/types.ts: context field added to SystemSnapshot

src/status/collector.ts: setContextEngine() method, context merged into
snapshot each poll, active_overlays surfaced from PolicyManager

src/tray/lifecycle.ts: ContextEngine + PolicyManager init, context_changed
event wires to applyContextOverlays, setContextEngine called post-init,
engine stopped on shutdown

src/status/server.ts: renderContext() JS function, context section HTML
(name, confidence chip, overlay badges), called from render(d)

TypeScript: 0 errors
