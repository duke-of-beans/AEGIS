feat(AEGIS-INTEL-01): per-drive disk I/O via WMI

Add wmi crate and disk_io module to replace hardcoded 0s in DiskMetrics
with real read/write bytes-per-second from Win32_PerfFormattedData_PerfDisk_LogicalDisk.

- src-tauri/Cargo.toml: wmi = "0.13" added
- src-tauri/src/disk_io.rs: new module — get_disk_io() returns HashMap<drive, (read,write)>;
  all WMI errors caught and logged, empty map returned on failure (never panics)
- src-tauri/src/main.rs: mod disk_io registered
- src-tauri/src/metrics.rs: collect_metrics() calls get_disk_io() once per cycle,
  looks up values per drive letter, graceful fallback to 0 if WMI unavailable

Quality gate: cargo check — 0 errors, 0 new warnings
