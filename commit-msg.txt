feat(mcp): AEGIS-MCP-02 — rich MCP publisher v3

src/mcp/server.ts: Full rewrite. v2 stub (5 tools) → v3 publisher (14 tools).

New tools: aegis_status (concise health), aegis_get_cognitive_load (score +
breakdown + interpretation), aegis_get_context (context + confidence + overlays),
aegis_get_system_snapshot (full snapshot), aegis_get_process_tree (spawn tree),
aegis_get_runaways (sniper watches with z-scores + escalation),
aegis_get_action_log (recent actions with reasons), aegis_get_confidence
(score + auto mode unlock status), aegis_get_session_summary (session stats),
aegis_apply_policy_overlay (programmatic context overlay for GREGORE),
aegis_preflight (audit + optimize machine for target context),
aegis_switch_profile, aegis_set_timer, aegis_cancel_timer.

setIntelligence() method accepts all v3 engines post-construction.
interpretLoad() maps score to human-readable pressure description.

src/tray/lifecycle.ts: setIntelligence() called with all engines after
full initialization — contextEngine, policyManager, sniperEngine,
learningStore, loadEngine.

TypeScript: 0 errors
