feat(learning): AEGIS-LEARN-01 — learning store + cognitive load engine

src/learning/store.ts: LearningStore — SQLite sessions.db, work_sessions +
action_outcomes + cognitive_load_samples + confidence_state tables.
startSession/endSession lifecycle (context-driven boundaries).
recordAction() → outcome ID. updateActionOutcome() implicit approval.
recordExplicitFeedback() with signal + intensity (strong=5x weight).
Confidence: (approvals - rejections*2) / total, auto unlocks at >=30 + >=0.75.
getConfidenceState(), getRecentSessions(), getBestSession().
Singleton initLearningStore()/getLearningStore().

src/learning/load.ts: CognitiveLoadEngine — 6-signal weighted composite 0-100.
CPU / memory / disk queue / DPC rate / runaway count / tab count.
Normalized against reference values. Equal weights at launch.
getTier() → green/amber/red. compute() returns full LoadBreakdown.

src/config/types.ts: cognitive_load + confidence added to SystemSnapshot

src/status/server.ts: POST /feedback route, onFeedback() callback,
renderLoad() (header badge with tier color), renderConfidence()
(progress bar + decisions until auto), both in render(d)

src/tray/lifecycle.ts: learning store + load engine init, setLearningEngine()
on collector, session started at startup, context_changed boundaries,
onFeedback wired to recordExplicitFeedback(), store stopped on shutdown

src/status/collector.ts: setLearningEngine(), load computed each poll,
load sample recorded, confidence state in snapshot

TypeScript: 0 errors
