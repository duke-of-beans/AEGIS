feat(ui): AEGIS-UI-01 — Command Surface Redesign + Installer

src/status/html.ts: New file. Full HTML cockpit builder.
  Design: deep midnight blue base, cold electric cyan, amber heat,
  red danger. JetBrains Mono → Consolas cascade. CRT scanline overlay,
  grid atmosphere, phosphor glow on cognitive load score.

  Layout: 3-column cockpit. Left panel (194px): CPU/RAM/DiskQ/DPC meter
  bars with GPU when available, context + active overlays, profile
  switcher, active timer. Center panel (flex): process tree as default
  view with full parent-child hierarchy (│ ├─ └─), tabs for DRIVES /
  NETWORK / GPU / BROWSER tabs. Right panel (264px): permanent action
  log with reasoning text + feedback buttons, confidence bar.

  Process tree: full spawn hierarchy reconstruction from process_tree
  array. Runaway processes highlighted amber (WATCH) or red (CRITICAL)
  with deviation indicator (△/▲). Genuinely better than Process Lasso.

  Action log: each entry shows time ago, process name, action badge
  (THROTTLE/SUSPEND/KILL), and the full reason text — reasoning not
  conclusions. Feedback buttons wire to POST /feedback.

  Cognitive load score: hero element. Big glowing number, color-shifts
  green/amber/red with phosphor text-shadow. Updates every 3s.

  ASCII art: structural — panel headers use ┌─ SECTION ─── style
  box-drawing. Process tree uses full │ ├─ └─ hierarchy characters.
  Wordmark ▌AEGIS▐. Not cliché — earned.

  Footer: version, uptime, watch count, last updated.
  Profile switch: modal input overlay, keyboard-navigable.
  Spinner: ◐◓◑◒ cycling at 700ms.

src/status/server.ts: Added import for buildStatusHtml from html.ts.

installer/install.ps1: Rich PowerShell installer.
  Banner: full 6-line block-art AEGIS wordmark (██╗  ██╗ style, cyan
  border, white letters). Prerequisite check (Node 18+, npm, git, pm2).
  6 steps: copy, npm install + build, pm2 start + save, startup task,
  claude_desktop_config.json MCP entry, open status window.
  Completion ceremony with status/logs/MCP quick-reference.

TypeScript: 0 errors
