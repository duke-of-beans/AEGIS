AEGIS-COCKPIT-02: complete cockpit rewrite — all systems functional

Tab navigation fixed (sel() globally scoped), light mode persists to
localStorage, process action buttons execute Tauri IPC with visible
feedback (no silent failures), sniper canvas animation live, profile
override demoted to bottom panel, font sizes increased, IPC field
mapping corrected (Rust → SNAP), DOMContentLoaded init wired.

Root cause: COCKPIT-01 session was interrupted — the final script block
(modals, IPC init, toggleTheme, DOMContentLoaded) was never written.
This sprint adds the complete missing block and corrects the tooltip
block to match spec.

Quality: npm run lint 0 errors, npx tsc --noEmit 0 errors.
